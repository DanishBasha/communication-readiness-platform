{
  "name": "communication-readiness-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Institutional Communication Readiness & Placement Preparation Platform",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
